package com.yifang.exchange.stockfilter;

import org.springframework.data.jpa.repository.JpaRepository;

public interface StockFilterResultRepository extends JpaRepository<StockFilterResult, Long> {
}
